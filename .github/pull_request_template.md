## What this changes

## Why

## How it was tested

- [ ] `python -m unittest discover -s tests` passes
- [ ] `ruff check src tests` passes
- [ ] No credentials, tokens or personal document content in the diff

## Notes for reviewers

If this touches the unofficial API behaviour, please also update `docs/mubu-api-notes.md` and
mention it in `CHANGELOG.md`.
